[phases.setup]
nixPkgs = ['nodejs', 'npm']

[phases.install]
cmds = ['cd backend && npm install --production']

[phases.build]
cmds = ['echo "Build completed"']

[start]
cmd = 'cd backend && npm start'