{
  "name": "tetiano-system",
  "version": "1.0.0",
  "description": "TETIANO System - Shopify Store Management with Admin and Customer Dashboards",
  "main": "backend/src/server.js",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm start",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install --production && npm start",
    "railway:build": "cd backend && npm install --production",
    "railway:start": "cd backend && npm start"
  },
  "keywords": [
    "shopify",
    "ecommerce",
    "management",
    "dashboard",
    "tetiano"
  ],
  "author": "TETIANO Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
